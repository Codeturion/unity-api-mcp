# Unity version to serve: "2022", "2023", or "6" (default: auto-detect or "6")
# UNITY_VERSION=6

# Override Unity install path (auto-detected if not set, used by ingest only)
# UNITY_INSTALL_PATH=H:/Unity/6000.3.8f1

# Path to a Unity project (used for version auto-detection + package source parsing)
# UNITY_PROJECT_PATH=F:/Unity Projects/my-project
